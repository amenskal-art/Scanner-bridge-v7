org.gradle.jvmargs=-Xmx3072m -Dfile.encoding=UTF-8
android.useAndroidX=true
# AGP 8 defaults to non-transitive R classes; the AUSBC library code references
# resources from its dependencies through its own R class (e.g. material's
# design_bottom_sheet), which requires the old transitive behavior.
android.nonTransitiveRClass=false
